{
  "name": "~dev",
  "version": "1.21.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "scripts/dev",
    "dev:app": "yarn workspace app run dev",
    "dev:simulateur": "yarn workspace simulateur run dev",
    "dev:up": "docker-compose up --build",
    "dev:prod": "docker-compose -f docker-compose.build.yaml up --build",
    "lint": "yarn workspaces run lint",
    "fresh-install": "scripts/fresh-install",
    "check-all": "scripts/check-all",
    "postinstall": "scripts/postinstall",
    "test:api:docker": "docker exec -it egapro-next_api_1 /usr/local/bin/py.test -vv --cov",
    "test:api": "cd packages/api && EGAPRO_DBPORT=5436 EGAPRO_DBNAME=test_egapro ./venv/bin/py.test -vv --cov"
  },
  "devDependencies": {
    "@types/node": "17.0.17",
    "@types/react": "17.0.39",
    "eslint": "8.9.0",
    "eslint-config-next": "12.0.10",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-jest": "^25.2.2",
    "eslint-plugin-jest-dom": "^4.0.1",
    "eslint-plugin-prettier": "^4.0.0",
    "eslint-plugin-testing-library": "^5.0.5",
    "prettier": "^2.5.1",
    "typescript": "4.5.5"
  },
  "dependencies": {
    "concurrently": "^7.2.2"
  }
}
